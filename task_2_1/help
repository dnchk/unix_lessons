Usage: task [OPTION] [DIR]
Sort all files in DIR to subdirs with name corresponding to extension of the
appropriate file.

With no DIR provided assume DIR is current directory.

    -h, --help		display this help and exit

Examples:
    task taskdir	Sort all files in taskdir.
    task --help		Output this help.
